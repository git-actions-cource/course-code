name: First workflow

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello Hithub"
      - name: Multiline command
        run: |
          node -v
          npm -v
